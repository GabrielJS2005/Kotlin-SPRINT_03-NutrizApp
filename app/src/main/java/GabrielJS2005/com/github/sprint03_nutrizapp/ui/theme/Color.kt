package GabrielJS2005.com.github.sprint03_nutrizapp.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Primary — Azul petróleo suave ───────────────────────────────────────────
val BluePetroleum     = Color(0xFF3D8EA0)   // primary
val BluePetroleumDark = Color(0xFF1E5D6A)   // primary dark (dark mode)
val BluePetroleumSoft = Color(0xFFE3F2F6)   // primary-soft (backgrounds pastéis)

// ─── Accent — Verde água ──────────────────────────────────────────────────────
val GreenWater        = Color(0xFF5CB8B2)   // accent / secondary
val GreenWaterSoft    = Color(0xFFE6F5F4)   // accent-soft

// ─── Coral — Rosa/coral emocional ─────────────────────────────────────────────
val CoralSoft         = Color(0xFFE87461)   // coral
val CoralSoftBg       = Color(0xFFFDF0EE)   // coral-soft (background pastel)
val CoralForeground   = Color(0xFFFFFFFF)   // coral-foreground

// ─── Mint — Verde positivo ─────────────────────────────────────────────────────
val Mint              = Color(0xFF5CBD9B)   // mint
val MintSoft          = Color(0xFFE5F5EF)   // mint-soft (background pastel)
val MintForeground    = Color(0xFF1E5F44)   // mint-foreground

// ─── Estados semânticos ────────────────────────────────────────────────────────
val SuccessGreen      = Color(0xFF4BAD7A)   // success
val SuccessGreenSoft  = Color(0xFFE4F5EC)   // success-soft
val SuccessFg         = Color(0xFFFFFFFF)   // success-foreground

val WarningAmber      = Color(0xFFC9913A)   // warning
val WarningAmberSoft  = Color(0xFFF9F0E2)   // warning-soft
val WarningFg         = Color(0xFF5C3D0F)   // warning-foreground

val InfoBlue          = Color(0xFF4A87C8)   // info
val InfoBlueSoft      = Color(0xFFE5EEFA)   // info-soft
val InfoFg            = Color(0xFFFFFFFF)   // info-foreground

val ErrorRed          = Color(0xFFD45244)   // destructive
val ErrorRedSoft      = Color(0xFFFAE5E3)   // destructive-soft

// ─── Neutros / Backgrounds ────────────────────────────────────────────────────
val OffWhite          = Color(0xFFF0F7F9)   // background (azulado, alinhado ao Lovable)
val SurfaceWhite      = Color(0xFFFFFFFF)   // card / surface
val BorderColor       = Color(0xFFDCEDF2)   // border
val InputBackground   = Color(0xFFEBF3F7)   // input bg

// ─── Texto ────────────────────────────────────────────────────────────────────
val TextDark          = Color(0xFF243044)   // foreground (azul escuro)
val TextGray          = Color(0xFF4A5568)   // body text
val TextGrayLight     = Color(0xFF7D9AAE)   // muted-foreground

// ─── Dark Mode ────────────────────────────────────────────────────────────────
val DarkBackground    = Color(0xFF111E28)   // dark background
val DarkSurface       = Color(0xFF1A2A38)   // dark card/surface
val DarkBorder        = Color(0xFF243242)   // dark border
val DarkForeground    = Color(0xFFEEF5F8)   // dark foreground

// Alias para compatibilidade retroativa
val BackgroundWhite   = SurfaceWhite
val PinkSoft          = Color(0xFFF4A7B9)
